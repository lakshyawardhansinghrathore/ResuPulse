fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
python-dotenv>=1.0
spacy>=3.7,<4.0
# NOTE: torch is intentionally NOT listed here.
# The Dockerfile installs the CPU-only build of torch first (via pytorch.org/whl/cpu)
# so that sentence-transformers picks it up without pulling in 2 GB of CUDA libraries.
# Adding torch here would let pip override that with the full GPU build.
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1
python-magic>=0.4.27
jinja2>=3.1
weasyprint>=60.0
httpx>=0.27
groq>=0.13
PyJWT[crypto]>=2.8
